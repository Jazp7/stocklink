# Stocklink

A full-stack product and provider management system built as a technical assessment. Stocklink exposes a RESTful API and a React frontend to perform full CRUD operations on products and providers, including pagination, sorting, filtering, and field selection.

---

## Tech Stack

| Layer    | Technology                        |
|----------|-----------------------------------|
| Frontend | React + TypeScript, Vite, pnpm    |
| Backend  | Python, FastAPI, uv               |
| Database | PostgreSQL via Supabase            |

---

## Prerequisites

Make sure you have the following installed before running the project:

- [Node.js](https://nodejs.org/) (v18 or higher)
- [pnpm](https://pnpm.io/) — `npm install -g pnpm`
- [Python](https://www.python.org/) (v3.11 or higher)
- [uv](https://docs.astral.sh/uv/) — `pip install uv`
- A [Supabase](https://supabase.com/) account with the database tables created (see below)

---

## Installation

### 1. Clone the repository

```bash
git clone https://github.com/yourname/stocklink.git
cd stocklink
```

### 2. Set up the backend

```bash
cd backend
cp .env.example .env
# Fill in your environment variables in .env
uv sync
```

### 3. Set up the frontend

```bash
cd ../frontend
cp .env.example .env
# Fill in your environment variables in .env
pnpm install
```

---

## Environment Variables

### Backend (`backend/.env`)

```bash
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres
SECRET_KEY=your_super_secret_random_string_here
ENV_MODE=development
```

### Frontend (`frontend/.env`)

```bash
VITE_API_URL=http://127.0.0.1:8000
```

---

## Database Setup

The database is hosted on Supabase. To initialize the tables, go to your Supabase project → **SQL Editor** → run the following:

```sql
-- 1. Create Providers Table
CREATE TABLE providers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    address VARCHAR(255),
    phone VARCHAR(50),
    description TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 2. Create Products Table
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    stock_quantity INT NOT NULL,
    category VARCHAR(50),
    description TEXT,
    provider_id INTEGER REFERENCES providers(id) ON DELETE SET NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
```

---

## Running Locally

### Start the backend

```bash
cd backend
uv run uvicorn app.main:app --reload
```

API will be available at: `http://127.0.0.1:8000`
Interactive docs at: `http://127.0.0.1:8000/docs`

### Start the frontend

```bash
cd frontend
pnpm dev
```

Frontend will be available at: `http://localhost:5173`

---

## API Documentation

Full interactive documentation is auto-generated by FastAPI and available at `/docs` when the backend is running locally.

### Endpoints

| Method | Endpoint        | Description           |
|--------|-----------------|-----------------------|
| GET    | /products       | List all products     |
| GET    | /products/:id   | Get single product    |
| POST   | /products       | Create product        |
| PUT    | /products/:id   | Update product        |
| DELETE | /products/:id   | Delete product        |
| GET    | /providers      | List all providers    |
| GET    | /providers/:id  | Get single provider   |
| POST   | /providers      | Create provider       |
| PUT    | /providers/:id  | Update provider       |
| DELETE | /providers/:id  | Delete provider       |

### Query Parameters (list endpoints)

| Parameter    | Example                        | Description              |
|--------------|--------------------------------|--------------------------|
| page         | `?page=2`                      | Page number              |
| limit        | `?limit=20`                    | Items per page           |
| sort         | `?sort=price` or `?sort=-price`| Sort ascending/descending|
| fields       | `?fields=id,name,price`        | Field selection          |
| name         | `?name=laptop`                 | Filter by name           |
| price[gte]   | `?price[gte]=100`              | Filter price range       |
| price[lte]   | `?price[lte]=500`              | Filter price range       |

---

## Testing

### API Testing (Postman)

1. Import the file `postman_collection.json` from the root of the repo into Postman
2. Set the `base_url` environment variable to `http://127.0.0.1:8000`
3. Run the collection

### Frontend Testing (Vitest)

```bash
cd frontend
pnpm test
```

---

## Deployment

| Service  | Platform        | URL                        |
|----------|-----------------|----------------------------|
| Frontend | Vercel          | _to be added_              |
| Backend  | Render / Railway| _to be added_              |
| Database | Supabase        | Managed — no URL needed    |

---

## Known Issues & Limitations

- No authentication or user accounts (by design — not required for this assessment).
- RLS (Row Level Security) is disabled on Supabase tables.
- No image upload support for products.

---

## Architecture Decisions

See [DECISIONS.md](./DECISIONS.md) for a full explanation of every major technical choice made in this project.
